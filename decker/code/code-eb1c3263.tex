\documentclass{standalone}
\usepackage[boxruled,lined]{algorithm2e}
\usepackage{mathtools}
\usepackage{amsfonts} 
\usepackage[svgnames]{xcolor}

\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}
\SetKwInput{KwInput}{Input}
\SetKwInput{KwOutput}{Output}
\begin{document}
\pagestyle{empty}
\begin{algorithm}[H]
  \KwInput{Step size $\alpha \in (0, 1]$, and a small $\epsilon > 0$.}
  \textcolor{blue}{Initialize $Q(s,a)$ for all $s \in \mathcal S^+$, $a \in \mathcal A(s)$,   arbitrarily except that $Q(\texttt{terminal}, \cdot) = 0$.\\}
\For{each episode} {
  \textcolor{red}{Initialize $S$} \\
  \tcp{SARSA:\textcolor{blue}{Choose $A$ from $S$ using policy derived from $Q$}}
  \For{each step of the episode, until $S$ is terminal} {
\textcolor{Green}{Choose $A$ from $S$ using policy derived from $Q$ (e.g. $\epsilon$-greedy)} \\
\textcolor{red}{Take action $A$, observe $R, S'$ \\}
\tcp{Key difference from Sarsa is in next step, was $\color{blue}Q(S', A')$}
\textcolor{blue}{
    $Q(S,A) \gets Q(S,A) + \alpha \left[R + \gamma \color{Green}{\max_a Q(S', a)} \color{blue} -       Q(S,A)\right]$ \\}
    \textcolor{Green}{$S \gets S'$} \tcp{SARSA was also doing \textcolor{blue}{$A \gets A'$}}
  }
}
\end{algorithm}
\end{document}